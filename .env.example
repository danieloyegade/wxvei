# Optional: serve project videos from external storage instead of the repo.
# When this is set, any video path under /projects/.../videos/ is resolved
# against this base URL. Leave it empty to use local files in public/projects.
# Example: https://media.example.com
PUBLIC_PROJECT_VIDEO_BASE_URL=
