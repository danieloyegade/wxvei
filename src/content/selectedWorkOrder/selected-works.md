---
beats:
  - template: "opening"
    projects:
      - "jamine"
      - "reny"
  - template: "solo"
    projects:
      - "cheri"
  - template: "balanced"
    projects:
      - "someplace-else"
      - "mia"
  - template: "solo"
    projects:
      - "Winter notes"
  - template: "solo"
    projects:
      - "abiola-for-w-models"
  - template: "solo"
    projects:
      - "isaac"
  - template: "closing"
    projects:
      - "public-illumination-studies"
      - "saucony"
  - template: "solo"
    projects:
      - "dj-paullette-for-seen-mag"
  - template: "solo"
    projects:
      - "moving-images-in-g-sharp-minor"
  - template: "solo"
    projects:
      - "of-the-sublime-and-beautiful"
  - template: "solo"
    projects:
      - "lola"
---

Edit the `beats` list above to change the selected works order.

Each project is referenced by its slug, which matches the project URL. Move a slug to a different beat to move it on the page. Use `solo` for one-project rows, or one of `opening`, `reverse`, `balanced`, `split`, and `closing` for two-project rows.
